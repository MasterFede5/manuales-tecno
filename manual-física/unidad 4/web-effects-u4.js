// EFECTOS WEB - UNIDAD 4: IoT/Control - Engranaje tecnológico
let particles = [], mouseX = innerWidth / 2, mouseY = innerHeight / 2, canvas, ctx, creatureX, creatureY, rotation = 0, time = 0;
function initWebEffects() { canvas = document.createElement('canvas'); canvas.id = 'particles-canvas'; canvas.style.cssText = 'position:fixed;top:0;left:0;width:100%;height:100%;z-index:9999;pointer-events:none;'; document.body.appendChild(canvas); ctx = canvas.getContext('2d'); creatureX = innerWidth / 2; creatureY = innerHeight / 2; resizeCanvas(); initParticles(); addNavigation(); addDarkBg(); animate(); addEventListener('resize', () => { resizeCanvas(); initParticles() }); document.addEventListener('mousemove', e => { mouseX = e.clientX; mouseY = e.clientY }); document.addEventListener('touchmove', e => { if (e.touches.length > 0) { mouseX = e.touches[0].clientX; mouseY = e.touches[0].clientY } }) }
function addDarkBg() { const bg = document.createElement('div'); bg.style.cssText = 'position:fixed;top:0;left:0;width:100%;height:100%;background:linear-gradient(135deg,#0a0a1a 0%,#0B1F3A 50%,#1a0a2e 100%);z-index:-1;'; document.body.insertBefore(bg, document.body.firstChild) }
function addNavigation() { if (document.querySelector('.web-nav-bar')) return; const nav = document.createElement('nav'); nav.className = 'web-nav-bar'; nav.innerHTML = `<div class="web-nav-content"><a href="../index-web.html" class="web-nav-logo">ALBATROS <span>FÍSICA</span></a><div class="web-nav-links"><a href="../index-web.html">Inicio</a><a href="../unidad 1/index.html">U1</a><a href="../unidad 2/index.html">U2</a><a href="../unidad 3/index.html">U3</a><a href="../unidad 4/index.html" style="background:#0B1F3A;color:#fff;">U4</a><a href="../ANEXOS/index.html">Anexos</a></div></div>`; const style = document.createElement('style'); style.textContent = `.web-nav-bar{position:fixed;top:0;left:0;right:0;z-index:10000;background:rgba(11,31,58,0.95);backdrop-filter:blur(15px);padding:10px 20px}.web-nav-content{max-width:1200px;margin:0 auto;display:flex;justify-content:space-between;align-items:center}.web-nav-logo{font-family:Montserrat,sans-serif;font-weight:900;font-size:1rem;color:#fff;text-decoration:none}.web-nav-logo span{color:#FF6F00}.web-nav-links{display:flex;gap:5px}.web-nav-links a{color:#E0E6ED;text-decoration:none;font-family:Montserrat,sans-serif;font-size:.7rem;font-weight:600;padding:5px 10px;border-radius:12px;background:rgba(255,255,255,.1);transition:.3s}.web-nav-links a:hover{background:#B71C1C}body{padding-top:50px}.page{max-width:900px;margin:0 auto;background:rgba(255,255,255,0.95);border-radius:12px;box-shadow:0 10px 40px rgba(0,0,0,0.3)}@media print{#particles-canvas,.web-nav-bar{display:none!important}body{padding-top:0}.page{max-width:none;box-shadow:none}}`; document.head.appendChild(style); document.body.insertBefore(nav, document.body.firstChild) }
function resizeCanvas() { canvas.width = innerWidth; canvas.height = innerHeight }
function createParticle() { return { x: Math.random() * canvas.width, y: Math.random() * canvas.height, size: Math.random() * 2 + 1, speedX: (Math.random() - .5) * .3, speedY: (Math.random() - .5) * .3, opacity: Math.random() * .4 + .1, hue: Math.random() > 0.5 ? 200 : 30 } }
function initParticles() { particles = []; for (let i = 0; i < 60; i++)particles.push(createParticle()) }
function updateParticles() { particles.forEach(p => { p.x += p.speedX; p.y += p.speedY; if (p.x < 0) p.x = canvas.width; if (p.x > canvas.width) p.x = 0; if (p.y < 0) p.y = canvas.height; if (p.y > canvas.height) p.y = 0 }) }
function drawParticles() { particles.forEach(p => { ctx.beginPath(); ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2); ctx.fillStyle = `hsla(${p.hue},80%,60%,${p.opacity})`; ctx.fill() }) }
function drawGear(x, y, r, teeth, rot) { ctx.save(); ctx.translate(x, y); ctx.rotate(rot); ctx.beginPath(); for (let i = 0; i < teeth * 2; i++) { const a = (i * Math.PI) / teeth; const rd = i % 2 === 0 ? r : r - 6; ctx.lineTo(Math.cos(a) * rd, Math.sin(a) * rd) } ctx.closePath(); ctx.fillStyle = 'rgba(50,100,150,0.8)'; ctx.fill(); ctx.strokeStyle = 'rgba(100,200,255,0.8)'; ctx.lineWidth = 2; ctx.stroke(); ctx.beginPath(); ctx.arc(0, 0, r * .3, 0, Math.PI * 2); ctx.fillStyle = 'rgba(11,31,58,0.9)'; ctx.fill(); ctx.restore() }
function drawCreature() { time += .05; const dx = mouseX - creatureX, dy = mouseY - creatureY; creatureX += dx * .06; creatureY += dy * .06; rotation += .02 + Math.sqrt(dx * dx + dy * dy) * .0005; ctx.beginPath(); ctx.arc(creatureX, creatureY, 45, 0, Math.PI * 2); const glow = ctx.createRadialGradient(creatureX, creatureY, 0, creatureX, creatureY, 45); glow.addColorStop(0, 'rgba(0,200,255,0.3)'); glow.addColorStop(1, 'rgba(0,100,200,0)'); ctx.fillStyle = glow; ctx.fill(); drawGear(creatureX, creatureY, 25, 8, rotation); drawGear(creatureX + 22, creatureY - 16, 14, 6, -rotation * 1.3); for (let i = 0; i < 6; i++) { const a = (i / 6) * Math.PI * 2 + rotation * .1; const len = 32 + Math.sin(time * 2 + i) * 4; ctx.beginPath(); ctx.moveTo(creatureX + Math.cos(a) * 10, creatureY + Math.sin(a) * 10); ctx.lineTo(creatureX + Math.cos(a) * len, creatureY + Math.sin(a) * len); ctx.strokeStyle = `rgba(0,255,200,${.5 + Math.sin(time * 3 + i) * .3})`; ctx.lineWidth = 2; ctx.stroke(); ctx.beginPath(); ctx.arc(creatureX + Math.cos(a) * len, creatureY + Math.sin(a) * len, 3, 0, Math.PI * 2); ctx.fillStyle = 'rgba(0,255,200,0.8)'; ctx.fill() } }
function animate() { ctx.clearRect(0, 0, canvas.width, canvas.height); updateParticles(); drawParticles(); drawCreature(); requestAnimationFrame(animate) }
if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', initWebEffects); else initWebEffects();
